---
description: f103-cmsis-hal — CubeIDE 风格对照，最小 HAL 子集
globs: projects/f103-cmsis-hal/**
alwaysApply: false
---

# f103-cmsis-hal

- 保持 Cube 分层：`MX_*`、`stm32f1xx_hal_msp.c`、`stm32f1xx_it.c`、`stm32f1xx_hal_conf.h`
- 串口：`USART1_WriteStr` → `HAL_UART_Transmit`；**不要**为本 demo 加 `printf`/`syscalls.c`
- PC13：`__HAL_RCC_PWR_CLK_ENABLE` + `HAL_PWR_EnableBkUpAccess` 后再 `HAL_GPIO_Init`
- 时钟在 `main` 的 `SystemClock_Config`（CMSIS `SystemInit` 不配 PLL）
- `third_party/**` 只经 `fetch-f103-cmsis-hal-deps.sh` 更新；HAL Src **9** 个 `.c`；vendor 正文英文
- 改 HAL 模块须同步：`fetch` 的 `HAL_SRC_FILES`、`CMakeLists.txt`、`stm32f1xx_hal_conf.h`
- 文档：`doc/projects/f103-cmsis-hal.md`
